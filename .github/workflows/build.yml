name: BUILD - Code Validation and Artifact Creation

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: false
        type: choice
        options: [dev, staging, prod]
        default: dev
      force_build:
        description: 'Force build all components'
        required: false
        type: boolean
        default: false
  pull_request:
    branches: [main]
  push:
    branches: [main, 'feature/*', 'bugfix/*', 'hotfix/*']

permissions:
  id-token: write
  contents: read
  pull-requests: write
  security-events: write

env:
  AWS_DEFAULT_REGION: us-east-1
  TF_IN_AUTOMATION: true
  TF_CLI_ARGS: "-no-color"

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and Validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      build_id: ${{ steps.info.outputs.build_id }}
      environment: ${{ steps.info.outputs.environment }}
      has_terraform_changes: ${{ steps.changes.outputs.terraform }}
      has_content_changes: ${{ steps.changes.outputs.content }}
      has_workflow_changes: ${{ steps.changes.outputs.workflows }}
      artifacts_created: ${{ steps.artifacts.outputs.created }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build Info
        id: info
        run: |
          BUILD_ID="build-${{ github.run_id }}-${{ github.run_attempt }}"
          ENV="${{ github.event.inputs.environment || 'dev' }}"
          
          echo "build_id=$BUILD_ID" >> $GITHUB_OUTPUT
          echo "environment=$ENV" >> $GITHUB_OUTPUT
          
          echo "# 🏗️ BUILD Phase" >> $GITHUB_STEP_SUMMARY
          echo "- **Build ID**: $BUILD_ID" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: $ENV" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Event**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY

      - name: Detect Changes
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            terraform:
              - 'terraform/**'
            content:
              - 'src/**'
            workflows:
              - '.github/workflows/**'
            docs:
              - '**/*.md'
            tests:
              - 'test/**'

      - name: Setup Infrastructure Tools
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🔧 Infrastructure Tools Setup" >> $GITHUB_STEP_SUMMARY
          
          # Install OpenTofu
          curl -L -o /tmp/tofu.zip https://github.com/opentofu/opentofu/releases/download/v1.6.1/tofu_1.6.1_linux_amd64.zip
          unzip -q /tmp/tofu.zip -d /tmp
          sudo mv /tmp/tofu /usr/local/bin/
          echo "✅ OpenTofu installed: $(tofu version)" >> $GITHUB_STEP_SUMMARY

      - name: Validate Terraform
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        working-directory: terraform
        run: |
          echo "## ✅ Terraform Validation" >> $GITHUB_STEP_SUMMARY
          
          # Format check
          if tofu fmt -check -recursive; then
            echo "✅ Format check passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Format check failed - run 'tofu fmt -recursive'" >> $GITHUB_STEP_SUMMARY
            echo "**Files needing formatting:**" >> $GITHUB_STEP_SUMMARY
            tofu fmt -recursive -diff | head -20 >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          # Initialize without backend for validation
          cp backend.tf backend.tf.bak 2>/dev/null || true
          rm -f backend.tf
          
          if tofu init -backend=false; then
            echo "✅ Init successful" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Init failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          # Validate configuration
          if tofu validate; then
            echo "✅ Validation passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Validation failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          # Restore backend file
          mv backend.tf.bak backend.tf 2>/dev/null || true

      - name: Setup Security Tools
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🔧 Security Tools Setup" >> $GITHUB_STEP_SUMMARY
          
          # Install Checkov
          pip3 install checkov --quiet
          echo "✅ Checkov installed: $(checkov --version)" >> $GITHUB_STEP_SUMMARY
          
          # Install Trivy
          wget -qO- https://github.com/aquasecurity/trivy/releases/download/v0.48.3/trivy_0.48.3_Linux-64bit.tar.gz | tar xzf - -C /tmp
          sudo mv /tmp/trivy /usr/local/bin/
          echo "✅ Trivy installed: $(trivy --version)" >> $GITHUB_STEP_SUMMARY

      - name: Security Scanning - Checkov
        id: security-checkov
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🔒 Checkov Security Analysis" >> $GITHUB_STEP_SUMMARY
          
          # Run Checkov with JSON output
          if checkov -d terraform --framework terraform --output json --output-file checkov-results.json --skip-check CKV_AWS_20,CKV_AWS_117 --quiet; then
            CHECKOV_EXIT=0
          else
            CHECKOV_EXIT=1
          fi
          
          # Parse and display results
          if [ -f "checkov-results.json" ]; then
            # Count findings by severity
            CRITICAL_COUNT=$(jq '[.results.failed_checks[]? | select(.severity == "CRITICAL")] | length' checkov-results.json 2>/dev/null || echo "0")
            HIGH_COUNT=$(jq '[.results.failed_checks[]? | select(.severity == "HIGH")] | length' checkov-results.json 2>/dev/null || echo "0")
            MEDIUM_COUNT=$(jq '[.results.failed_checks[]? | select(.severity == "MEDIUM")] | length' checkov-results.json 2>/dev/null || echo "0")
            LOW_COUNT=$(jq '[.results.failed_checks[]? | select(.severity == "LOW")] | length' checkov-results.json 2>/dev/null || echo "0")
            TOTAL_FAILED=$(jq '.results.failed_checks | length' checkov-results.json 2>/dev/null || echo "0")
            TOTAL_PASSED=$(jq '.results.passed_checks | length' checkov-results.json 2>/dev/null || echo "0")
            
            # If no severity classification, treat all as medium
            if [ "$CRITICAL_COUNT" = "0" ] && [ "$HIGH_COUNT" = "0" ] && [ "$MEDIUM_COUNT" = "0" ] && [ "$LOW_COUNT" = "0" ] && [ "$TOTAL_FAILED" -gt "0" ]; then
              MEDIUM_COUNT=$TOTAL_FAILED
            fi
            
            TOTAL_FINDINGS=$((CRITICAL_COUNT + HIGH_COUNT + MEDIUM_COUNT + LOW_COUNT))
            
            # Display detailed results
            echo "### 📊 Checkov Results Summary" >> $GITHUB_STEP_SUMMARY
            echo "- 🔴 **Critical**: $CRITICAL_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- 🟠 **High**: $HIGH_COUNT findings" >> $GITHUB_STEP_SUMMARY  
            echo "- 🟡 **Medium**: $MEDIUM_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- 🔵 **Low**: $LOW_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ **Passed**: $TOTAL_PASSED checks" >> $GITHUB_STEP_SUMMARY
            echo "- **Total Issues**: $TOTAL_FINDINGS" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Show sample findings if any exist
            if [ "$TOTAL_FINDINGS" -gt "0" ]; then
              echo "### 🔍 Sample Checkov Findings (Top 5)" >> $GITHUB_STEP_SUMMARY
              jq -r '.results.failed_checks[]? | "- **" + (.check_id // "Unknown") + "**: " + (.check_name // "No description") + " (File: " + (.file_path // "Unknown") + ")"' checkov-results.json 2>/dev/null | head -5 >> $GITHUB_STEP_SUMMARY || echo "- No detailed findings available" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "❌ Checkov results file not found" >> $GITHUB_STEP_SUMMARY
            CHECKOV_EXIT=1
          fi
          
          # Create summary artifact
          cat > checkov-security-summary.md << EOF
          # Checkov Security Analysis Report
          
          **Build ID**: ${{ steps.info.outputs.build_id }}
          **Scan Date**: $(date -u)
          **Status**: $([ $CHECKOV_EXIT -eq 0 ] && echo "PASSED" || echo "FAILED")
          
          ## Summary
          - **Critical**: $CRITICAL_COUNT
          - **High**: $HIGH_COUNT  
          - **Medium**: $MEDIUM_COUNT
          - **Low**: $LOW_COUNT
          - **Total Issues**: $TOTAL_FINDINGS
          - **Passed Checks**: $TOTAL_PASSED
          EOF
          
          # Set output for next steps
          echo "checkov_exit=$CHECKOV_EXIT" >> $GITHUB_OUTPUT
          echo "checkov_critical=$CRITICAL_COUNT" >> $GITHUB_OUTPUT
          echo "checkov_high=$HIGH_COUNT" >> $GITHUB_OUTPUT
          echo "checkov_total=$TOTAL_FINDINGS" >> $GITHUB_OUTPUT
          
          if [ $CHECKOV_EXIT -eq 0 ]; then
            echo "✅ **Checkov scan completed successfully**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Checkov found security issues**" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Security Scanning - Trivy
        id: security-trivy
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🛡️ Trivy Security Analysis" >> $GITHUB_STEP_SUMMARY
          
          # Run Trivy with JSON output
          if trivy fs --security-checks vuln,config --format json --output trivy-results.json terraform/; then
            TRIVY_EXIT=0
          else
            TRIVY_EXIT=1
          fi
          
          # Parse and display results
          if [ -f "trivy-results.json" ]; then
            # Count findings by severity
            CRITICAL_COUNT=$(jq '[.Results[]?.Misconfigurations[]? | select(.Severity == "CRITICAL")] | length' trivy-results.json 2>/dev/null || echo "0")
            HIGH_COUNT=$(jq '[.Results[]?.Misconfigurations[]? | select(.Severity == "HIGH")] | length' trivy-results.json 2>/dev/null || echo "0")
            MEDIUM_COUNT=$(jq '[.Results[]?.Misconfigurations[]? | select(.Severity == "MEDIUM")] | length' trivy-results.json 2>/dev/null || echo "0")
            LOW_COUNT=$(jq '[.Results[]?.Misconfigurations[]? | select(.Severity == "LOW")] | length' trivy-results.json 2>/dev/null || echo "0")
            TOTAL_FINDINGS=$((CRITICAL_COUNT + HIGH_COUNT + MEDIUM_COUNT + LOW_COUNT))
            
            # Display detailed results  
            echo "### 📊 Trivy Results Summary" >> $GITHUB_STEP_SUMMARY
            echo "- 🔴 **Critical**: $CRITICAL_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- 🟠 **High**: $HIGH_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- 🟡 **Medium**: $MEDIUM_COUNT findings" >> $GITHUB_STEP_SUMMARY  
            echo "- 🔵 **Low**: $LOW_COUNT findings" >> $GITHUB_STEP_SUMMARY
            echo "- **Total Issues**: $TOTAL_FINDINGS" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Show sample findings if any exist
            if [ "$TOTAL_FINDINGS" -gt "0" ]; then
              echo "### 🔍 Sample Trivy Findings (Top 5)" >> $GITHUB_STEP_SUMMARY
              jq -r '.Results[]?.Misconfigurations[]? | "- **" + (.ID // "Unknown") + "**: " + (.Title // "No description") + " (Severity: " + (.Severity // "Unknown") + ")"' trivy-results.json 2>/dev/null | head -5 >> $GITHUB_STEP_SUMMARY || echo "- No detailed findings available" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "❌ Trivy results file not found" >> $GITHUB_STEP_SUMMARY
            TRIVY_EXIT=1
          fi
          
          # Create summary artifact
          cat > trivy-security-summary.md << EOF
          # Trivy Security Analysis Report
          
          **Build ID**: ${{ steps.info.outputs.build_id }}
          **Scan Date**: $(date -u)
          **Status**: $([ $TRIVY_EXIT -eq 0 ] && echo "PASSED" || echo "FAILED")
          
          ## Summary  
          - **Critical**: $CRITICAL_COUNT
          - **High**: $HIGH_COUNT
          - **Medium**: $MEDIUM_COUNT
          - **Low**: $LOW_COUNT
          - **Total Issues**: $TOTAL_FINDINGS
          EOF
          
          # Set output for next steps
          echo "trivy_exit=$TRIVY_EXIT" >> $GITHUB_OUTPUT
          echo "trivy_critical=$CRITICAL_COUNT" >> $GITHUB_OUTPUT
          echo "trivy_high=$HIGH_COUNT" >> $GITHUB_OUTPUT
          echo "trivy_total=$TOTAL_FINDINGS" >> $GITHUB_OUTPUT
          
          if [ $TRIVY_EXIT -eq 0 ]; then
            echo "✅ **Trivy scan completed successfully**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Trivy found security issues**" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Process Security Results
        if: steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🏁 Security Results Summary" >> $GITHUB_STEP_SUMMARY
          
          # Get results from previous steps
          CHECKOV_EXIT="${{ steps.security-checkov.outputs.checkov_exit || '0' }}"
          TRIVY_EXIT="${{ steps.security-trivy.outputs.trivy_exit || '0' }}"
          CHECKOV_CRITICAL="${{ steps.security-checkov.outputs.checkov_critical || '0' }}"
          TRIVY_CRITICAL="${{ steps.security-trivy.outputs.trivy_critical || '0' }}"
          CHECKOV_HIGH="${{ steps.security-checkov.outputs.checkov_high || '0' }}"
          TRIVY_HIGH="${{ steps.security-trivy.outputs.trivy_high || '0' }}"
          
          TOTAL_CRITICAL=$((CHECKOV_CRITICAL + TRIVY_CRITICAL))
          TOTAL_HIGH=$((CHECKOV_HIGH + TRIVY_HIGH))
          SECURITY_ERRORS=0
          
          # Apply blocking logic for critical/high findings
          if [ $TOTAL_CRITICAL -gt 0 ]; then
            echo "🔴 **CRITICAL SECURITY ISSUES**: $TOTAL_CRITICAL findings" >> $GITHUB_STEP_SUMMARY
            SECURITY_ERRORS=$((SECURITY_ERRORS + 1))
          fi
          
          if [ $TOTAL_HIGH -gt 0 ]; then
            echo "🟠 **HIGH SECURITY ISSUES**: $TOTAL_HIGH findings" >> $GITHUB_STEP_SUMMARY
            SECURITY_ERRORS=$((SECURITY_ERRORS + 1))
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Overall Security Status" >> $GITHUB_STEP_SUMMARY
          echo "| Scanner | Status | Critical | High |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|----------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| Checkov | $([ $CHECKOV_EXIT -eq 0 ] && echo "✅ PASS" || echo "❌ FAIL") | $CHECKOV_CRITICAL | $CHECKOV_HIGH |" >> $GITHUB_STEP_SUMMARY
          echo "| Trivy   | $([ $TRIVY_EXIT -eq 0 ] && echo "✅ PASS" || echo "❌ FAIL") | $TRIVY_CRITICAL | $TRIVY_HIGH |" >> $GITHUB_STEP_SUMMARY
          echo "| **Total** | **$([ $SECURITY_ERRORS -eq 0 ] && echo "✅ PASS" || echo "❌ FAIL")** | **$TOTAL_CRITICAL** | **$TOTAL_HIGH** |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Final decision
          if [ $SECURITY_ERRORS -gt 0 ]; then
            echo "🚨 **BUILD FAILED** - Critical or High security findings detected" >> $GITHUB_STEP_SUMMARY
            echo "**Action Required**: Fix security issues before deployment" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "✅ **All security scans passed** - No critical or high severity findings" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Validate Website Content
        if: steps.changes.outputs.content == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 🌐 Website Content Validation" >> $GITHUB_STEP_SUMMARY
          
          # Check required files
          REQUIRED_FILES=("src/index.html" "src/404.html" "src/robots.txt")
          for file in "${REQUIRED_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ $file exists" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ $file missing" >> $GITHUB_STEP_SUMMARY
              exit 1
            fi
          done
          
          # Basic HTML validation
          if command -v html5validator >/dev/null 2>&1 || pip3 install html5validator --quiet; then
            if html5validator --root src/ --also-check-css; then
              echo "✅ HTML validation passed" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ HTML validation warnings (non-blocking)" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Create Artifacts
        id: artifacts
        if: steps.changes.outputs.content == 'true' || steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true'
        run: |
          echo "## 📦 Creating Build Artifacts" >> $GITHUB_STEP_SUMMARY
          
          # Create website archive
          if [ -d "src" ]; then
            tar -czf website-${{ steps.info.outputs.build_id }}.tar.gz -C src .
            echo "✅ Website archive created" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Create terraform archive
          if [ -d "terraform" ]; then
            tar -czf terraform-${{ steps.info.outputs.build_id }}.tar.gz terraform/
            echo "✅ Terraform archive created" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "created=true" >> $GITHUB_OUTPUT

      - name: Upload Artifacts
        if: steps.artifacts.outputs.created == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ steps.info.outputs.build_id }}
          path: |
            website-*.tar.gz
            terraform-*.tar.gz
            *-security-summary.md
            checkov-results.json
            trivy-results.json
          retention-days: 7

      - name: Build Summary
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Terraform | ${{ steps.changes.outputs.terraform == 'true' && '✅ Validated' || '➖ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "| Website | ${{ steps.changes.outputs.content == 'true' && '✅ Validated' || '➖ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "| Security | ${{ (steps.changes.outputs.terraform == 'true' || github.event.inputs.force_build == 'true') && '🔒 Enhanced Analysis - Passed' || '➖ Skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "| Artifacts | ${{ steps.artifacts.outputs.created == 'true' && '✅ Created' || '➖ None needed' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ job.status }}" = "success" ]; then
            echo "🎉 **BUILD Successful** - Ready for TEST phase" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **BUILD Failed** - Check logs above for details" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi